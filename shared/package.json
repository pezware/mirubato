{
  "name": "@mirubato/shared",
  "version": "0.1.0",
  "description": "Shared types and utilities for Mirubato",
  "type": "module",
  "main": "types/index.ts",
  "types": "types/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "echo 'No tests for shared types'",
    "test:unit": "echo 'No tests for shared types'",
    "test:integration": "echo 'No tests for shared types'",
    "test:coverage": "echo 'No tests for shared types'"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
