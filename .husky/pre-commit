#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged for formatting and linting
npx lint-staged

# Run unit tests before commit
npm run test:unit -- --passWithNoTests --watchAll=false

# Check TypeScript types (skip for now due to test type issues)
# npm run type-check