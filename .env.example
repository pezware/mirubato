# Rubato Environment Variables Template
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local or any file with actual secrets

# Frontend Environment Variables (prefix with VITE_ for Vite to expose them)
VITE_API_BASE_URL=
VITE_AUDIO_SAMPLES_URL=
VITE_ENVIRONMENT=development
VITE_SENTRY_DSN=

# Backend Environment Variables (Cloudflare Workers)
# These are configured in Cloudflare dashboard or wrangler secrets
# Do NOT put these in .env files - use wrangler secrets instead:
# DATABASE_URL=
# JWT_SECRET=
# RESEND_API_KEY=
# CLOUDFLARE_API_TOKEN=